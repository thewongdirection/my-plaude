# Core (required): the ASR engine. Handles WAV/MP3 decoding internally via PyAV.
faster-whisper>=1.0.0

# --- Optional extras (install only if you need them) ---
# Better neural denoising (`--denoise deepfilter`); also installs torch:
#   pip install deepfilternet
#
# Speaker diarization (`--diarize`); also installs torch and needs a free
# Hugging Face token on first run. Two backend choices:
#   pip install "pyannote.audio>=3.1"      # --diarize-backend pyannote (default)
#   pip install "whisperx>=3.1"            # --diarize-backend whisperx
#
# Summarization (`--summarize`) needs NO extra pip package (uses stdlib urllib),
# but does need a local LLM server running - either:
#   Ollama:    https://ollama.com/download  (then `ollama pull llama3.1`)
#   llama.cpp: https://github.com/ggml-org/llama.cpp  (`llama-server -m model.gguf`)
#
# External binary (not a pip package) - required for reading audio:
#   FFmpeg   (Windows: `winget install ffmpeg`; Linux: `apt install ffmpeg`)
